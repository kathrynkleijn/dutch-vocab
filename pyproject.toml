[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "dutchvocab"
version = "0.1.0"

[project.scripts]
dutchlesson = "dutchvocab.__main__:main"

[tool.setuptools.packages.find]
where = ["."]
